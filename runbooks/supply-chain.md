# Supply-chain rejection

Trivy blocks HIGH/CRITICAL findings before signing. Cosign signs the pushed digest; admission accepts only signatures matching `signing/cosign.pub` in protected namespaces.

For an unfixed vendor CVE, create a time-limited ADR with CVE, impact, compensating control, owner, and expiry. Do not weaken the cluster-wide threshold silently.
